import Component from "site/components/search/Sort.tsx";
import type { Props } from "site/components/search/Sort.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
